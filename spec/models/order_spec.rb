require 'rails_helper'

RSpec.describe Order, type: :model do
  # it "create should be valid" do
  #   expect(build(:order)).to be_valid
  # end

  # it "null user is not valid" do
  #   expect( build(:order, organization: nil) ).to_not be_valid
  # end

  # it "null address is not valid" do
  #   expect( build(:order, address: nil) ).to_not be_valid
  # end

  # it "create with null ride should be valid" do
  #   expect( build(:order, ride: nil) ).to be_valid
  #   expect( build(:order, ride: nil) ).to be_valid
  # end

  # it "negative distance_from_src is not valid" do
  #   expect{ create(:order, distance_from_src: -1, organization: Organization.create) }.to raise_error
  # end
end
